services:
  postgres:
    image: postgres:17-alpine
    container_name: fila-certa-postgres
    environment:
      POSTGRES_DB: filacerta
      POSTGRES_USER: filacerta
      POSTGRES_PASSWORD: filacerta
    ports:
      - "5432:5432"
    volumes:
      - fila_certa_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U filacerta -d filacerta"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  fila_certa_postgres_data:
